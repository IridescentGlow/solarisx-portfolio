# Vite client build — canonical site origin, no trailing slash. See src/lib/seo.js.
VITE_SITE_URL=

# api/contact.mjs (server-side only, never exposed to the client).
# See docs/engineering/CONTACT_FORM_ARCHITECTURE.md.
RESEND_API_KEY=
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL=
